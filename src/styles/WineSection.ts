import styled from 'styled-components';

const WineSectionStyledComponent = styled.section`
    display: flex;
    align-items: center;
    justify-content: center;
    flex-direction: row;
    flex-wrap: wrap;
    width: 1200px;
`;

export default WineSectionStyledComponent;
